"""Trip Agent core package: orchestrator, tools, LLM client, schemas, and logging.

Declared as a regular package so `from agent.agent import run_agent` resolves
reliably inside Vercel's bundled serverless functions.
"""
